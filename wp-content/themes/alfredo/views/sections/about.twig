<section class="py-5">
    <div class="container"> 
        <div class="row mx-0">
            <div class="col-lg-6 col-12">
                <h2>{{about.title}}</h2>
                {{about.text}}
            </div>
            <div class="col-lg-6 col-12">
                <img src="{{Image(about.image).src}}" alt="{{about.image.alt}}" class="img-responsive">
            </div>
        </div>
    </div>
</section>